# Exact versions used to produce every number and figure in the paper
# (see results/environment/environment.json for the full fingerprint).
# Newer minor versions will very likely work but have not been verified.
torch==2.10.0
numpy==2.0.2
pandas==2.3.3
scipy==1.16.3
matplotlib==3.10.0
